// Expected metadata file name inside each child app folder
export const APP_MANIFEST_FILE = 'app.json';

// Fallback visual configuration if an app is missing an explicit icon
export const DEFAULT_APP_ICON = '<path d="M4 4h16v16H4z"></path>';

